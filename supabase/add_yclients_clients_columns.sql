-- Add missing YClients-compatible fields to public.clients.
-- Existing columns are preserved; only new columns are added.

ALTER TABLE public.clients
  ADD COLUMN IF NOT EXISTS yclients_id BIGINT,
  ADD COLUMN IF NOT EXISTS name TEXT,
  ADD COLUMN IF NOT EXISTS surname TEXT,
  ADD COLUMN IF NOT EXISTS patronymic TEXT,
  ADD COLUMN IF NOT EXISTS display_name TEXT,
  ADD COLUMN IF NOT EXISTS email TEXT,
  ADD COLUMN IF NOT EXISTS card TEXT,
  ADD COLUMN IF NOT EXISTS birth_date TEXT,
  ADD COLUMN IF NOT EXISTS comment TEXT,
  ADD COLUMN IF NOT EXISTS discount NUMERIC DEFAULT 0,
  ADD COLUMN IF NOT EXISTS visits INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS sex_id INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS sex TEXT,
  ADD COLUMN IF NOT EXISTS sms_check INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS sms_bot INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS sms_not INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS spent NUMERIC DEFAULT 0,
  ADD COLUMN IF NOT EXISTS paid NUMERIC DEFAULT 0,
  ADD COLUMN IF NOT EXISTS balance NUMERIC DEFAULT 0,
  ADD COLUMN IF NOT EXISTS importance_id INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS importance TEXT,
  ADD COLUMN IF NOT EXISTS categories JSONB DEFAULT '[]'::jsonb,
  ADD COLUMN IF NOT EXISTS last_change_date TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS custom_fields JSONB DEFAULT '{}'::jsonb;

COMMENT ON COLUMN public.clients.yclients_id IS 'Original numeric client ID from YClients payload data.id';
